xxxx'use client';

import DashboardLayout from '@/components/DashboardLayout';
import Portfolio from '@/components/Portfolio';
import TradeBlotter from '@/components/TradeBlotter';
import PositionsTable from '@/components/PositionsTable';
import TradeHistory from '@/components/TradeHistory';
import IndicatorsTable from '@/components/IndicatorsTable';


export default function DashboardPage() {
    return (
        <DashboardLayout>
            <div className="space-y-8">
                {/* Header */}
                <div>
                    <h1 className="text-3xl font-bold text-white">Dashboard</h1>
                    <p className="text-gray-400 mt-1">
                        Overview of your portfolio, positions, trades, and indicators
                    </p>
                </div>

                {/* Top grid: Portfolio + Quick Trade */}
                <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
                    {/* Portfolio (charts + summary) */}
                    <div className="lg:col-span-2 space-y-4">
                        <Portfolio /> {/* Chart */}
                        <Portfolio /> {/* Summary table / metrics */}
                    </div>

                    {/* TradeBlotter */}
                    <div className="lg:col-span-1">
                        <TradeBlotter />
                    </div>
                </div>

                {/* Middle grid: Positions + Trade History */}
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                    <PositionsTable />
                    <TradeHistory />
                </div>

                {/* Bottom: Indicators */}
                <div className="space-y-4">
                    <h2 className="text-xl font-semibold text-white">Indicators</h2>
                    <IndicatorsTable />
                </div>
            </div>
        </DashboardLayout>
    );
}
